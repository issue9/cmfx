languages:
    - zh-Hans
    - cmn-Hans
    - zh-CN
messages:
    - key: |
          ## system stat json
          %s
      message:
          msg: |
              ## 系统状态的 JSON 格式数据
              %s
    - key: TOTP passport
      message:
          msg: TOTP 验证
    - key: The ID of passport
      message:
          msg: 登录验证器的 ID
    - key: The api doc of %s
      message:
          msg: "%s 的 API 文档"
    - key: The description of passport
      message:
          msg: 登治验证器的描述
    - key: add admin api
      message:
          msg: 添加管理员
    - key: add currency value
      message:
          msg: 增加的金额
    - key: add member level api
      message:
          msg: 添加会员等级
    - key: add member type api
      message:
          msg: 添加会员类型
    - key: add role api
      message:
          msg: 添加管理员角色
    - key: admin module
      message:
          msg: 管理员模块
    - key: admin sse server
      message:
          msg: 管理端的 SSE 服务
    - key: admin tag
      message:
          msg: 后台管理端的所有接口
    - key: audit setting
      message:
          msg: 审核设置
    - key: auth tag
      message:
          msg: 登录验证
    - key: auto register with %s
      message:
          msg: 由 %s 触发而自动注册
    - key: avatar
      message:
          msg: 头像
    - key: backup api
      message:
          msg: 备份数据库
    - key: backup database
      message:
          msg: 备份数据库
    - key: bad request body not allowed
      message:
          msg: 不被允许的的提交内容
    - key: bad request body not allowed detail
      message:
          msg: 不被允许的的提交内容，比如上传了不允许的文件格式等。
    - key: bad request invalid body
      message:
          msg: 无效的报文
    - key: bad request invalid body
      message:
          msg: 提交的内容格式不正确
    - key: bad request invalid body detail
      message:
          msg: 无效的内容格式或是内容与客户端指定的 content-type 不相符。
    - key: bad request invalid header
      message:
          msg: 无效的报头
    - key: bad request invalid header detail
      message:
          msg: |
              无效的报头内容，缺少必要的报头，或是提交的报头格式不正确返回此错误。
    - key: bad request invalid query
      message:
          msg: 无效的查询参数
    - key: bad request invalid query detail
      message:
          msg: |
              查询参数格式不正确，类似于无效的路径参数，大概率是前端提交的类型有误。
    - key: balance not enough
      message:
          msg: 余额不足
    - key: before bind need create secret
      message:
          msg: 绑定之前需要先创建密钥
    - key: before bind need create secret detail
      message:
          msg: 在使用绑定接口绑定 TOTP 之前，需要调用创建密钥的接口生成一密钥。
    - key: bind %s
      message:
          msg: 绑定 %s
    - key: bind %s passport for current user api
      message:
          msg: 为当前用户绑定 %s 验证方式
    - key: birthday
      message:
          msg: 生日
    - key: can not add user with %s state
      message:
          msg: 在 %s 状态下不能添加用户
    - key: change current user password for %s passport api
      message:
          msg: 修改当前用户的 %s 验证方式的密码
    - key: change password
      message:
          msg: 修改密码
    - key: code
      message:
          msg: 验证码
    - key: code receiver, ignore when binded
      message:
          msg: 验证码接收者，如果已经绑定，则会忽略此值
    - key: create sse token api
      message:
          msg: 生成用于访问 SSE 接口的令牌
    - key: created time
      message:
          msg: 创建时间
    - key: currency value after action
      message:
          msg: 操作之后的金额
    - key: currency value before action
      message:
          msg: 操作之前的金额
    - key: del backup database file
      message:
          msg: 删除备份的数据库文件
    - key: delete %s
      message:
          msg: 删除 %s
    - key: delete %s passport for current user api
      message:
          msg: 解绑当前用户与 %s 验证方式
    - key: delete admins
      message:
          msg: 删除管理员
    - key: delete backup file api
      message:
          msg: 删除备份文件
    - key: delete member
      message:
          msg: 删除会员
    - key: delete role api
      message:
          msg: 删除管理员角色
    - key: delete roles
      message:
          msg: 删除角色
    - key: delete secret for %s passport api
      message:
          msg: 删除登录适配器 %s 的安全码
    - key: delete the admin api
      message:
          msg: 删除管理员
    - key: delete the member api
      message:
          msg: 删除会员
    - key: edit role info api
      message:
          msg: 编辑角色信息
    - key: edit role resources api
      message:
          msg: 编辑角色的资源
    - key: edit roles
      message:
          msg: 修改角色信息
    - key: expire must b after now
      message:
          msg: 过期时间必须在此之前
    - key: expired in seconds
      message:
          msg: 过期时间（秒）
    - key: forbidden can not delete yourself
      message:
          msg: 不允许删除自身
    - key: forbidden can not delete yourself detail
      message:
          msg: 不允许删除自身
    - key: forbidden must be author
      message:
          msg: 只有作者才能进行此操作
    - key: forbidden must be author detail
      message:
          msg: 没有足够的权限，该操作只有作者本身才能执行。
    - key: forbidden state not allow
      message:
          msg: 当前状态不允许该操作
    - key: forbidden state not allow detail
      message:
          msg: 当前状态不允许该操作
    - key: general setting
      message:
          msg: 常规设置
    - key: generator code for %s
      message:
          msg: 为 %s 生成验证码
    - key: get SSE message api
      message:
          msg: 访问 SSE 的接口
    - key: get admin info api
      message:
          msg: 获取管理员的信息
    - key: get admin list api
      message:
          msg: 获取管理员列表
    - key: get admins
      message:
          msg: 查看管理员信息
    - key: get api list api
      message:
          msg: 获取所有 API 的信息列表
    - key: get backup database list
      message:
          msg: 查看备份数据库的文件列表
    - key: get backup file list api
      message:
          msg: 获取备份文件列表
    - key: get login user info api
      message:
          msg: 获取当前登录用户的信息
    - key: get login user security log api
      message:
          msg: 获取当前登录用户的安全日志列表
    - key: get member info api
      message:
          msg: 查看会员信息
    - key: get member invited api
      message:
          msg: 获得被当前会员邀请的会员
    - key: get member level list api
      message:
          msg: 获取会员等级列表
    - key: get member list api
      message:
          msg: 获得会员列表
    - key: get member statistic
      message:
          msg: 获得会员统计信息
    - key: get member type list api
      message:
          msg: 获得会员类型列表
    - key: get members
      message:
          msg: 查看会员信息
    - key: get passports list api
      message:
          msg: 获取支持验证方式列表
    - key: get resources list api
      message:
          msg: 获取资源列表
    - key: get role resources api
      message:
          msg: 获取角色资源的列表
    - key: get roles list api
      message:
          msg: 获取角色列表
    - key: get services list api
      message:
          msg: 获取服务列表
    - key: get system info api
      message:
          msg: 获取系统信息
    - key: get system problems api
      message:
          msg: 获取所有的错误代码
    - key: has been bind code
      message:
          msg: 验证码验证方式已经绑定
    - key: has been bind code detail
      message:
          msg: 该方式的验证已经绑定，一般多次调用绑定接口可能会发生此错误
    - key: has been bind to other account
      message:
          msg: 已经绑定到其它账号上
    - key: has been bind totp detail
      message:
          msg: 该账号已经绑定 TOTP
    - key: has been totp bind
      message:
          msg: 该账号已经绑定 TOTP
    - key: id
      message:
          msg: ID
    - key: identity registrable
      message:
          msg: |
              可注册的 ID
    - key: identity registrable detail
      message:
          msg: |
              某些登录状态验证失败之后，会返回一个可用于注册的 ID，客户端可根据此 ID 注册新的账号。
    - key: invalid url format
      message:
          msg: 无效的 URL 格式
    - key: inviter
      message:
          msg: 邀请人
    - key: keyword filter
      message:
          msg: 关键字过滤
    - key: lock the admin api
      message:
          msg: 锁定管理员
    - key: lock the member api
      message:
          msg: 锁定会员
    - key: log IP
      message:
          msg: log IP
    - key: log content
      message:
          msg: log content
    - key: log user agent
      message:
          msg: log user agent
    - key: login by %s
      message:
          msg: 以 %s 的验证方式登录
    - key: login by %s api
      message:
          msg: 以 %s 方式登录
    - key: logout api
      message:
          msg: 退出当前登录
    - key: member module
      message:
          msg: 会员模块
    - key: member tag
      message:
          msg: 会员
    - key: memo of action
      message:
          msg: 此操作的备注
    - key: must be a dir
      message:
          msg: 必须得是个目录
    - key: must be between (%v,%v)
      message:
          msg: 值必须介于 (%v,%v) 之间
    - key: must be between [%v,%v]
      message:
          msg: 值必须介于 [%v,%v] 之间
    - key: must be empty
      message:
          msg: 必须为空
    - key: must be greater than %v
      message:
          msg: 须大于 %v
    - key: must be less than %v
      message:
          msg: 须小于 %v
    - key: new password
      message:
          msg: 新密码
    - key: nickname
      message:
          msg: 昵称
    - key: not exists
      message:
          msg: 不存在
    - key: not found
      message:
          msg: 未找到
    - key: not found invalid path
      message:
          msg: 无效的路径参数
    - key: not found invalid path detail
      message:
          msg: |
              无效的路径参数，一般是路径参数的格式不正常，比如要求是数值型的，提交了 undefined， 比如 `/users/1` 变成了 `/users/undefined`。
    - key: old password
      message:
          msg: 旧密码
    - key: passkey begin login for %s api
      message:
          msg: "%s 的预登录"
    - key: passkey delete credential for %s api
      message:
          msg: 删除 %s 的证书
    - key: passkey delete for %s api
      message:
          msg: 删除 %s 的登录账号
    - key: passkey get credentials for %s api
      message:
          msg: 获取 %s 的证书列表
    - key: passkey login for %s api
      message:
          msg: 完成 %s 登录
    - key: passport
      message:
          msg: 验证方式
    - key: passport id
      message:
          msg: 登录适配器的 ID
    - key: passport password mode
      message:
          msg: 密码
    - key: password
      message:
          msg: password
    - key: patch admin info api
      message:
          msg: 更新管理员信息
    - key: patch login user info api
      message:
          msg: 更新登不用户个人信息
    - key: patch member level api
      message:
          msg: 更新会员等级的提示信息
    - key: patch member type api
      message:
          msg: 更新会员类型的提示信息
    - key: post admins
      message:
          msg: 添加管理员
    - key: post roles
      message:
          msg: 添加角色
    - key: precondition failed need sse
      message:
          msg: 先决定条件 SSE 未运行
    - key: precondition failed need sse detail
      message:
          msg: 先决定条件 SSE 未运行
    - key: |
          problems response:

          %s
      message:
          msg: |
              每个错误对象的 type 表示错误代码，可能是以下的值之一:

              %s
    - key: psskey begin register for %s api
      message:
          msg: psskey begin register for %s api
    - key: psskey register for %s api
      message:
          msg: 向 %s 注册
    - key: put admin
      message:
          msg: 修改管理员信息
    - key: put member
      message:
          msg: 修改会员信息
    - key: put roles resources
      message:
          msg: 调整角色资源
    - key: end time must be after start time
      message:
          msg: 结束时间必须大于开始时间
    - key: rbac tag
      message:
          msg: RBAC 角色权限
    - key: refresh token
      message:
          msg: 刷新令牌
    - key: refresh token api
      message:
          msg: 刷新令牌
    - key: register member api
      message:
          msg: 注册会员
    - key: register successful
      message:
          msg: 会员注册成功
    - key: |-
          registered sse protocol:
          %s
      message:
          msg: |-
              已注册的 SSE 协议:
              %s
    - key: request code for %s passport bind api
      message:
          msg: 为绑定 %s 验证方式请求验证码
    - key: request code for %s passport login api
      message:
          msg: 为 %s 验证方式登录请求验证码
    - key: request secret for %s passport api
      message:
          msg: 为 %s 验证方式请求密钥
    - key: role
      message:
          msg: 角色
    - key: role description
      message:
          msg: role description
    - key: role id
      message:
          msg: role id
    - key: role name
      message:
          msg: role name
    - key: role parent
      message:
          msg: role parent
    - key: roles not exists
      message:
          msg: 角色不存在
    - key: root module
      message:
          msg: 根模块
    - key: secret expired
      message:
          msg: TOTP 密钥过期
    - key: secret expired detail
      message:
          msg: TOTP 密钥过期
    - key: set member level
      message:
          msg: 设置会员等级
    - key: set member level api
      message:
          msg: 设置会员等级
    - key: set member type
      message:
          msg: 设置会员类型
    - key: set member type api
      message:
          msg: 设置会员类型
    - key: settings tag
      message:
          msg: 设置
    - key: sex
      message:
          msg: 性别
    - key: state
      message:
          msg: 状态
    - key: strength invalid
      message:
          msg: 密码强度不够
    - key: subscribe system stat api
      message:
          msg: 订阅系统状态的 SSE 服务
    - key: system module
      message:
          msg: 系统模块
    - key: system tag
      message:
          msg: 系统
    - key: target
      message:
          msg: 接收者
    - key: the ID of admin
      message:
          msg: 管理员 ID
    - key: the ID of member
      message:
          msg: 会员 ID
    - key: the ID of member level
      message:
          msg: the ID of member level
    - key: the ID of member type
      message:
          msg: 会员的 ID 值
    - key: the backup filename
      message:
          msg: 备份文件的文件名
    - key: the file name
      message:
          msg: 文件名
    - key: the id of credential
      message:
          msg: 证书 ID
    - key: the new password can not be equal old
      message:
          msg: 新旧密码不能相同
    - key: the role id
      message:
          msg: 角色 ID
    - key: the state for passport and identity
      message:
          msg: 表示适配器与当前 ID 的状态，每个适配器表示的值是不同的。
    - key: the value not in candidate
      message:
          msg: 该值并不在候选列表中
    - key: token auth
      message:
          msg: 令牌凭证登录
    - key: totp code
      message:
          msg: TOTP 验证码
    - key: totp secret
      message:
          msg: TOTP 密钥
    - key: type of action
      message:
          msg: 操作的类型
    - key: unauthorized invalid account
      message:
          msg: 账号或是密码错误
    - key: unauthorized invalid account detail
      message:
          msg: 账号不存在或是当前账号与密码不匹配
    - key: unauthorized invalid state
      message:
          msg: 当前状态无法登录
    - key: unauthorized invalid state detail
      message:
          msg: |
              登录账号的状态无效，比如账号被锁定导致无法登录等。
    - key: unauthorized invalid token
      message:
          msg: 登陆失效
    - key: unauthorized invalid token detail
      message:
          msg: 登录令牌过期，长时间不登录会返回此错误。
    - key: unauthorized need change password
      message:
          msg: 只有修改密码才能进行其它操作
    - key: unauthorized need change password detail
      message:
          msg: |
              只有修改密码才能继续其它操作，一般是被定义了该账号的密码已经不安全了，需要重新设置密码。
    - key: unauthorized security token
      message:
          msg: 需要强验证
    - key: unauthorized security token detail
      message:
          msg: 当前请求需要重新验证用户信息。
    - key: unlock the admin api
      message:
          msg: 解锁管理员
    - key: unlock the member api
      message:
          msg: 解锁会员
    - key: unsubscribe system stat api
      message:
          msg: 取消系统状态的 SSE 服务
    - key: update info
      message:
          msg: 更新信息
    - key: upload file
      message:
          msg: 上传文件
    - key: upload module
      message:
          msg: 上传模块
    - key: upload static server
      message:
          msg: 已上传文件的访问接口
    - key: upload tag
      message:
          msg: 上传
    - key: url blacklist filter
      message:
          msg: URL 黑名单过滤
    - key: user id
      message:
          msg: user id
    - key: user identity for current passport
      message:
          msg: 当前适配器对应用户的账号
    - key: user logout
      message:
          msg: 用户主动退出
    - key: user no
      message:
          msg: user no
    - key: user state
      message:
          msg: user state
    - key: user token
      message:
          msg: 用户令牌
    - key: username
      message:
          msg: 账号
    - key: username %s exists
      message:
          msg: 用户名 %s 已经存在
    - key: view apis
      message:
          msg: 查看接口信息
    - key: view services
      message:
          msg: 查看服务列表
    - key: view system info
      message:
          msg: 查看系统信息
    - key: view system stat
      message:
          msg: 查看系统状态
    - key: webauthn passport
      message:
          msg: webauthn
