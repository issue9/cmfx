
#### 数据表格
用于展示数据，但是不支持分页。
@```demo-basic```@

#### 表格
基本的表格功能，与 HTML 的 table 相同，加上了部分控制功能。
@```demo-table```@

#### 动态加载
用于展示数据，可通过方法加载数据内容，支持分页。
@```demo-loader```@

#### 分页
用于展示数据，可通过方法加载数据内容，支持分页。
@```demo-paging```@

@```api```@
