
#### locale
可通过 `LocaleProvider` 的属性修改所包含内容的语言。
@```demo-locale```@

#### global
可通过 `setLocale` 修改全局的本地化内容。
@```demo-global```@

#### 其它属性
@```demo-other```@

#### 嵌套使用
@```demo-nested```@

@```api```@
