# 支持的平台

## 前端

- Node >= 24.0.0；
- Solid >= 1.9.0；
- Typescript >= 6.0；

### 浏览器

Safari、Chorme 和 Firefox 最后两个版本都没有问题。
具体可参考根目录下的 `.browserslistrc`。

## 后端

- Go >= 1.26；
- sqlite3 >= 3.0；
- postgresql >= 12.0；
- mysql >= 8.0;
- mariadb >= 10.2;
